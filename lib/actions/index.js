'use strict';

var Reflux = require('reflux');

var ServerStatsActions = Reflux.createActions([
/**
 * define your actions as strings below, for example:
 */
'toggleStatus']);

module.exports = ServerStatsActions;